# Adapter 转换测试文档

这是一段普通的段落文本，包含 **加粗**、*斜体*、~~删除线~~、`行内代码` 和 [链接](https://example.com) 等行内格式。

## 二级标题

### 三级标题带 **加粗** 和 `代码`

> 这是一段引用文本，包含 **加粗** 和 *斜体*。

---

## 列表测试

### 无序列表（嵌套）

- 一级项目 A
  - 二级项目 A-1
    - 三级项目 A-1-a
    - 三级项目 A-1-b，带 **加粗** 文本
  - 二级项目 A-2
- 一级项目 B
- 一级项目 C
  - 二级项目 C-1

### 有序列表（嵌套）

1. 第一步：初始化项目
   1. 安装依赖 `npm install`
   2. 配置环境变量
      1. 创建 `.env` 文件
      2. 填写 API Key
2. 第二步：编写代码
3. 第三步：测试部署

### 待办事项（嵌套）

- [x] 已完成：搭建项目框架
  - [x] 初始化仓库
  - [ ] 编写 README
- [ ] 未完成：实现核心功能
  - [x] 数据模型设计
  - [ ] API 接口开发
    - [ ] 用户模块
    - [x] 权限模块

### 混合列表类型

- 无序项 1
- 无序项 2

2. 有序项 1
3. 有序项 2

- [x] 待办项 1
- [ ] 待办项 2

## 表格测试

### 简单表格

| 姓名 | 年龄 | 职位 |
|------|------|------|
| 张三 | 28 | 前端工程师 |
| 李四 | 32 | 后端工程师 |
| 王五 | 25 | 设计师 |

### 带格式的表格

| 功能 | 状态 | 说明 |
|------|------|------|
| **用户认证** | ~~已废弃~~ | 迁移到 OAuth2 |
| `数据缓存` | *开发中* | 使用 Redis |
| [API 文档](https://api.example.com) | 已完成 | 支持 Swagger |

### 大表格

| 模块 | 类型 | 优先级 | 负责人 | 截止日期                          | 备注 |
|------|------|--------|--------|-------------------------------|------|
| 适配器 | 重构 | P0 | 张三 | 2026<br/>-03-01               | 支持表格解析 |
| 列表 | Bug | P1 | 李四 | 2026-0<br/>2-281. sadasd | 嵌套层级丢失 |
|    |    |     |     |                               |    |
| 选区 | 新功能 | P2 | 王五 | 2026-03-15                    | 跨块选区 |
| 协同 | 优化 | P1 | 赵六 | 2026-03-10                    | 冲突解决策略 |

## 代码块测试

```typescript
interface BlockSnapshot {
  id: string;
  flavour: string;
  nodeType: 'root' | 'block' | 'editable' | 'void';
  props: Record<string, unknown>;
  children: BlockSnapshot[] | DeltaInsert[];
}
```

```python
def parse_markdown(text: str) -> dict:
    """解析 Markdown 文本"""
    import markdown
    return markdown.convert(text)
```

## 图片测试

![测试图片](https://example.com/test.png)

## 公式测试

### 块级公式

$$
E = mc^2
$$

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

### 行内公式

这是一段包含行内公式的文本：质能方程 $E = mc^2$ 是物理学中最著名的公式之一。

勾股定理 $a^2 + b^2 = c^2$ 和欧拉公式 $e^{i\pi} + 1 = 0$ 都是数学中的经典。

混合格式：**加粗文本**中嵌入公式 $\alpha + \beta = \gamma$，以及 `代码` 和 *斜体*。

## 综合嵌套场景

这段文字后面紧跟各种块类型：

- 列表项带 **加粗 *斜体嵌套* 结束**
  - 子项带 `代码` 和 [链接](https://example.com)

| 嵌套测试 | 内容 |
|----------|------|
| **加粗** + `代码` | ~~删除~~ + *斜体* |

> 引用块

---

最后一段普通文本，确认所有块类型都能正确解析和序列化。
