// 兼容旧源码路径；不在运行时类型目录重复定义内容模型。
export {InlineNodeType} from '@ccc/blockcraft/framework/model';
export type {
  IInlineNodeAttrs, InlineAttrKey, InlineDataKey, InlineStyleKey, InlineCustomKey,
  ITextStyles, IExpandedAttrs, InlineModel,
} from '@ccc/blockcraft/framework/model';
